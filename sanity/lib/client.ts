import { createClient } from 'next-sanity'

import { apiVersion, dataset, projectId } from '../env'

export const client = createClient({
  projectId,
  dataset,
  apiVersion,
  useCdn: false, // Set ke false supaya artikel baru langsung muncul saat di-publish tanpa delay cache
})
